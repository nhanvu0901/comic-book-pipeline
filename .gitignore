# Environment
.env
*.pyc
__pycache__/
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Outputs (stored in Google Drive)
*.mp4
*.wav
*.mp3
*.srt

# Temp
*.tmp
*.bak

# Stage logs
logs/
projects/
art_projects/
.understand-anything/
bin/
comic-pipeline-course/

# Research / analysis workspace (reference videos, gap reports, frame extracts)
research/
docs
tools/

# Session workspace + local backups (never commit)
scratchpad/
*.bak*
stage3-5-course/

# You.com scout run artifacts: raw API responses + the per-day search log. Grows with
# every run and reproduces nothing — the reports are read once and acted on.
scout_runs/

# Throwaway probes and their outputs (~37 MB of upscaler frames, crops, embedding
# caches). The scripts here are one-off experiments, not part of any pipeline run.
playground/
